export class LocalRoutes {

      static home = "/"

      static academiaId = "home-academia"
      static appsId = "home-apps"
      static blogId = "home-blog"
      static booksId = "home-books"
      static flashId = "home-flash-fiction"
      static miscId = "home-misc"
      static poetryId = "home-poetry"
      static songsId = "home-songs"
      static storiesId = "home-stories"

      static homeAcademia = `${LocalRoutes.home}#${LocalRoutes.academiaId}`
      static homeApps = `${LocalRoutes.home}#${LocalRoutes.appsId}`
      static homeBlog = `${LocalRoutes.home}#${LocalRoutes.blogId}`
      static homeBooks = `${LocalRoutes.home}#${LocalRoutes.booksId}`
      static homeFlashFiction = `${LocalRoutes.home}#${LocalRoutes.flashId}`
      static homeMisc = `${LocalRoutes.home}#${LocalRoutes.miscId}`
      static homePoetry = `${LocalRoutes.home}#${LocalRoutes.poetryId}`
      static homeSongs = `${LocalRoutes.home}#${LocalRoutes.songsId}`
      static homeStories = `${LocalRoutes.home}#${LocalRoutes.storiesId}`

      static academia = `/academia`
      static apps = `/apps`
      static blog = `/blog`
      static books = `/books`
      static flashFiction = `/flash-fiction`
      static misc = `/miscellany`
      static poetry = `/poetry`
      static songs = `/songs`
      static stories = `/stories`

      static about = `/about`
      static cv = `/cv`
      static settings = `/settings`
}
